# Next.js Turbopack resolves `next/package.json` from the app root; Yarn PnP
# does not expose a classic node_modules tree, which breaks that lookup.
# nodeLinker: node-modules keeps Yarn as the only package manager while matching
# what Next/Turbopack expect.
nodeLinker: node-modules
